from abc import abstractmethod
from Interface import Interface

class IManager:
    def __init__(self):
        ui_datbe = Interface()
        ui_home = Interface()
        ui_floor = Interface()
        ui_apart = Interface()
        ui_inhub = Interface()
        ui_infos = Interface()


